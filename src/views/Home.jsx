import { Text } from "react-native";
import AppWrapper from "./AppWrapper";

export default function Home ({}){
    return (
        <AppWrapper>
            <Text>
                {"Home view"}
            </Text>
        </AppWrapper>
    );
}